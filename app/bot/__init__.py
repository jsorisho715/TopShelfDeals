"""TopShelf Telegram bot package.

Mirrors the prototype behavior in ``topshelf/app/telegram.jsx``: every reply is a
ranked list best -> worst, each row carrying a Special (menu) link and a Route
(Google Maps directions) link, plus product photos so the user sees what they're
buying.
"""
